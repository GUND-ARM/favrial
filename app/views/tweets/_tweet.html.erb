<%= turbo_frame_tag(tweet) do %>
  <div class="col">
    <div class="card shadow-sm h-100">
      <div class="card-body">
        <!--<div class="d-flex align-items-center h-100">-->
        <div class="container">
          <div class="row">
            <div class="col">
              <blockquote class="twitter-tweet" data-conversation="none">
                <%= link_to tweet.t_id, tweet.url %>
              </blockquote>
            </div>
          </div>
          <div class="row align-items-end">
            <div class="col">
              <% if tweet.classification %>
                <% if tweet.slemio? %>
                  <p>このツィートは<strong>スレミオ</strong>だ。私がそう判断した</p>
                <% elsif tweet.suletta? %>
                  <p>このツィートは<strong>スレッタ</strong>だ。私がそう判断した</p>
                <% elsif tweet.miorine? %>
                  <p>このツィートは<strong>ミオリネ</strong>だ。私がそう判断した</p>
                <% else %>
                  <p>このツィートはスレミオ/スレッタ/ミオリネの<strong>いずれでもない。</strong>私がそう判断した</p>
                <% end %>
              <% else %>
                <%= render(ActionButtonComponent.new(tweet: tweet) ) %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
<% end %>
